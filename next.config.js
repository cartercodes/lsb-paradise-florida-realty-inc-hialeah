/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  basePath: '/lsb-paradise-florida-realty-inc-hialeah',
  images: { unoptimized: true },
};
module.exports = nextConfig;
